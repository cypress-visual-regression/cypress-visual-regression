{
  "name": "ts-alt",
  "version": "1.0.0",
  "description": "ts (no type: module) testing project",
  "scripts": {
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress-visual-regression": "../../../cypress-visual-regression",
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "cypress": ">=12"
  }
}
